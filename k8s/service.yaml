apiVersion: v1
kind: Service
metadata:
  name: cronsole-service
  namespace: default
spec:
  selector:
    app: cronsole
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
